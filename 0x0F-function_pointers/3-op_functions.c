/**
 * op_add - Function that add two number
 *
 * @a: gets the integers
 * @b: gets the integers
 *
 * Return: The sum of the of the integer
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - Function that subtract two number
 *
 * @a: gets the integers
 * @b: gets the integers
 *
 * Return: The subtraction of the integer
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - Function that multiply two number
 *
 * @a: gets the integers
 * @b: gets the integers
 *
 * Return: The multplication of the of the integer
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - Function that divide two number
 *
 * @a: gets the integers
 * @b: gets the integers
 *
 * Return: The division of the of the integer
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - Function that and two number
 *
 * @a: gets the integers
 * @b: gets the integers
 *
 * Return: The modulus of the of the integer
 */

int op_mod(int a, int b)
{
	return (a % b);
}
