#include <studio.h>
/**
 * main - A program thats prints on the terminal using printf
 *
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
