#include <stdio.h>
#include <ctype.h>
/**
 * _isupper - This Function checks for Uppercase and Lowercase
 *
 * @i: gets the argument from main.c
 * Return: (0)
 */
int _isupper(int i)
{
	if (isupper(i))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
